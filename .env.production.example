# PureLink production-like Docker Compose example.
# Copy this file before use:
#   cp .env.production.example .env.production
#
# Replace every "change-me" value before exposing the stack beyond localhost.

APP_NAME=PureLink
APP_ENV=production
APP_DEBUG=false
APP_VERSION=0.1.0
LOG_LEVEL=INFO

AUTH_SECRET_KEY=change-me-at-least-32-random-chars
AUTH_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

POSTGRES_DB=purelink
POSTGRES_USER=purelink
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_PORT=5432

REDIS_PORT=6379
APP_PORT=8000
FRONTEND_PORT=3000

DATABASE_URL=postgresql+psycopg://purelink:change-me-strong-password@db:5432/purelink
REDIS_URL=redis://redis:6379/0
DB_ECHO=false

API_BASE_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
FRONTEND_BASE_URL=http://localhost:3000

# Set this to your public frontend origin for a real deployment.
CORS_ORIGINS=http://localhost:3000
CORS_ALLOW_ORIGINS=http://localhost:3000
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*
CORS_ALLOW_CREDENTIALS=true

DATA_DIR=/app/data
UPLOAD_DIR=/app/data/uploads
PARSED_DIR=/app/data/parsed
CHUNKS_DIR=/app/data/chunks
CHUNK_DIR=/app/data/chunks
VECTOR_STORE_DIR=/app/data/vector_store

LLM_PROVIDER=heuristic
LLM_API_BASE_URL=
LLM_API_KEY=
DEEPSEEK_API_KEY=
LLM_MODEL=
LLM_TIMEOUT_SECONDS=30
LLM_REASONING_EFFORT=
LLM_THINKING_ENABLED=false

EMBEDDING_PROVIDER=fastembed
EMBEDDING_API_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
EMBEDDING_DEVICE=cpu
EMBEDDING_NORMALIZE=true
EMBEDDING_MODEL_CACHE_DIR=/app/models/embedding
EMBEDDING_TIMEOUT_SECONDS=30
EMBEDDING_BATCH_SIZE=32
EMBEDDING_DIMENSION=

CHUNK_STRATEGY=fixed
BLOCK_CHUNK_TARGET_CHARS=900
BLOCK_CHUNK_MAX_CHARS=1400
BLOCK_CHUNK_MIN_CHARS=120
BLOCK_CHUNK_TABLE_MAX_CHARS=1800
BLOCK_CHUNK_OVERLAP_CHARS=120

RERANKER_ENABLED=false
RERANKER_PROVIDER=noop
RERANKER_MODEL=

ENABLE_OCR=false
OCR_PROVIDER=disabled
OCR_LANG=eng
OCR_TESSERACT_COMMAND=tesseract
OCR_TESSERACT_PSM=6

ENABLE_MEDIA=false
ASR_PROVIDER=disabled
ASR_MODEL_PATH=/app/models/vosk
ASR_FFMPEG_COMMAND=ffmpeg
MULTIMODAL_PROVIDER=disabled

PROCESSING_QUEUE_KEY=purelink:processing-jobs:queued
PROCESSING_INFLIGHT_QUEUE_KEY=purelink:processing-jobs:inflight
PROCESSING_QUEUE_BLOCK_TIMEOUT_SECONDS=5
PROCESSING_JOB_TIMEOUT_SECONDS=1800
MAX_UPLOAD_SIZE_MB=50
MAX_ACTIVE_JOBS_PER_USER=5
MAX_ACTIVE_JOBS_PER_KB=10
